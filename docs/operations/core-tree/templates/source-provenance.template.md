# 來源追溯證據

- 輸入識別：
- 原始來源位置參考：<案件根目錄相對路徑，例如 ootb-r38\\tree>
- 複製或 export 方式：Export（案件預設值；若實際使用其他方式請替換）
- 蒐集日期時間（ISO 8601）：
- 保管人：
- 保管鏈參考：source-provenance.md、version-primary.md、integrity.md、integrity.sha256（案件預設關聯）
- 相關 `version-primary.*` 與 `integrity.*` 位置：
- 人工確認參考：BCO\kenny（案件操作者；確認日期與事項於執行時填寫）

填寫說明：

- 原始來源位置若位於 `K:\\70.ArasUpgradeCases` 下，填寫相對路徑，例如 `customer-11sp9\\tree`、`ootb-11sp9\\tree` 或 `ootb-r38\\tree`；完整路徑可另外列在備註。
- 複製方式預設填寫 `Export`；若實際使用 Windows 檔案總管複製／貼上或 `robocopy`，操作人員應改成實際方式。
- 保管鏈參考預設使用 `source-provenance.md`、`version-primary.md`、`integrity.md`、`integrity.sha256` 的關聯；若有案件／工單編號，再補充於此欄。
- 人工確認參考預設使用案件操作者；本範例為 `BCO\kenny`。確認日期與確認事項於執行時補填；不得將此預設值視為已完成正式核准。

本檔案僅為補充來源追溯，不能取代必要的版本與完整性證據檔案。
