# Plan: Core Tree execution Skill

1. Add a failing contract test for the Skill folder, metadata, dependency map, and missing-command stop boundary.
2. Add `SKILL.md`, `references/core-capabilities.md`, and `agents/openai.yaml`.
3. Record the independent Skill boundary in the Skill Map and design spec.
4. Implement the formal `CoreTreeComparisonCommand` and offline JSON test CLI.
5. Run the complete offline core test suite and Release build; keep UI and external adapters isolated.
